name: Android APK Build and Test

on:
  push:
    branches: [ "MaryJabro" ]
  pull_request:
    branches: [ "MaryJabro" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    # Set up JDK 17 for Android
    - name: Set up JDK 17
      uses: actions/setup-java@v3
      with:
        java-version: '17'
        distribution: 'temurin'

    # Set up Node.js for React Native
    - name: Set up Node.js
      uses: actions/setup-node@v3
      with:
        node-version: '16'

    # Install Node.js dependencies 
    - name: Install Node.js dependencies
      working-directory: VitalTrackApp  
      run: |
        npm install

    # Run Jest Unit Tests 
    - name: Run Unit Tests
      working-directory: VitalTrackApp  
      run: |
        npm run test

    # Build the Android APK (ensure the correct working directory)
    - name: Build Android APK
      working-directory: VitalTrackApp/android  
      run: |
        ./gradlew assembleDebug

 